	cst_sct3<> cst;
	construct_im(cst, texto, 1);

sdsl/construct.hpp - línea 67
Construye el índice de tipo cst_sct3 con el "texto" indicando 1 byte. 
Guarda el texto en un archivo temporal RAM y luego construye con:

construct(idx, tmp_file, num_bytes);

sdsl/construct.hpp - línea 67
Llama la función:
construct(idx, file, config, num_bytes);

sdsl/construct.hpp - línea 86
Llama la función:
construct(idx, file, config, num_bytes, index_tag);

sdsl/construct.hpp - línea 236
